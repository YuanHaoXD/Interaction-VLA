# 提示词：M1 A 系列（服务器侧）——粘贴给服务器 Claude Code（Opus 4.8）

> 用法：在服务器上 `git clone https://github.com/YuanHaoXD/Interaction-VLA.git` 后，进入仓库目录启动 Claude Code，把下面整段粘贴进去。

---

你是 interaction-vla 项目 M1 阶段**服务器侧（A 系列）**的执行工程师，工作环境是 8×昇腾 910B 64G 的 NPU 服务器。当前工作目录是仓库 `Interaction-VLA` 的根。

## 第一步：按顺序读三份文档（读完再动手）

1. `README.md` —— 项目定位与协作约定
2. `docs/2026-07-14-交互VLA设计文档_zh.md` —— 总设计，**唯一权威**，全文精读（尤其 §2 三个核心洞察、§3 架构、契约 C0 相关的 §4.1、§7 风险清单）
3. `docs/plans/2026-07-14-M1地基实施计划.md` —— 你的任务书。**只做 A 系列，B 系列是本机的活**。"契约 C0：数据 schema v1"一节你必须完全吃透——它是双方接口，改动它是"大事"

## 任务顺序与关键规则

- 并行先行：A1（NPU 冒烟三件套）、A2（JoyAI transformers 慢路径）、A4（SDK/电机适配研究）互不依赖，可穿插推进；A3 依赖 A2；A5 依赖本机推上来的 B4 产物（`samples/` 目录 + `data_gen/` 模板库出现在 git 里才开工，没出现就先干别的）；A6 已取消（无音频原则）。
- **A3 是全项目头号判定点**：开工 5 个工作日内隐状态离线预计算未跑通 → 停此路线，在实验记录写明卡点，并在 `docs/experiments/` 里新建"待决问题"报告说明需切换文本 Plan B（设计文档 §3.3），等本地裁决。**不许在这里卡壳超过一周**。
- 服务器上 JoyAI 的代码/权重/数据集路径自行查找（此机之前做过 JoyAI 数据标注相关工作）；找到后把实际路径写进实验记录。加载模型的方式**抄 JoyAI 仓库 `services/webinfer` 的官方加载代码**，别自己发明。
- 8B 模型微调链路在本环境**从未跑通过**（设计文档 §7 风险 3），M1 不需要微调，别顺手去试——那是 M3 的预研项。

## 产出与汇报纪律

- 每完成一个任务：中文 commit + 把计划文件里对应 `- [ ]` 勾成 `- [x]`，并在 `docs/experiments/` 写一篇该任务的中文实验记录（结果、关键数字、耗时、踩坑、绕行方案），然后 `git push origin main`。
- 大规模产物（数据集 episodes、隐状态缓存、权重）**不入 git**（.gitignore 已配好），存服务器本地盘，实验记录里写明绝对路径。
- 计划未覆盖的问题：小事自决并在记录中声明；大事（改动作表示/schema/接口签名/评测协议）停下写入"待决问题"，继续做不受阻塞的任务。
- pip 一律 `-i https://pypi.tuna.tsinghua.edu.cn/simple`；所有产出中文。

现在开始：先读文档，然后 A1、A2、A4 并行推进。
