# M1 地基阶段实施计划（2026-07 中 → 08 初）

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 打通交互 VLA 的全部地基：动作空间实测、轨迹模板库、数据 schema 与校验器、≥10 万段合成数据集 v1、JoyAI 隐状态离线预计算路径、NPU 冒烟测试。

**Architecture:** 见 [../2026-07-14-交互VLA设计文档_zh.md](../2026-07-14-交互VLA设计文档_zh.md)（唯一权威）。本阶段不训模型，只做数据与基础设施。

**Tech Stack:** Python 3.12、reachy-mini 1.8.0（MuJoCo 3.3.0 仿真）、numpy/scipy、torch + torch_npu（昇腾 910B）、transformers（JoyAI 8B 慢路径）、StarVLA（零件库）。

## Global Constraints（每个任务默认遵守）

- 所有产出（代码注释、commit message、报告）用**中文**。
- pip 一律加清华镜像：`-i https://pypi.tuna.tsinghua.edu.cn/simple`。
- 动作向量恒为 9 维、顺序固定：`[x, y, z, roll, pitch, yaw, body_yaw, ant_right, ant_left]`，单位米/弧度，30Hz，存储**不归一化**（归一化统计另存 `stats.json`）。
- 每完成一个任务：commit + 在 `docs/experiments/` 追加/更新该任务的实验记录（结果、耗时、踩坑），然后 push。
- 遇设计文档未覆盖的**大事**（动作表示/schema/评测协议改动）：停下，写入实验记录"待决问题"一节，继续做不受阻塞的其它任务。
- 本机（Windows）跑 Python 前设 `PYTHONUTF8=1`；连 SDK 前确保 `NO_PROXY` 含 localhost。

## 执行分工与依赖

- **B 系列 = 本机**（Windows 笔记本，有 MuJoCo 仿真环境：`E:\Github\ReachyMni_Project\reachy-mini-demo\.venv`）。
- **A 系列 = 服务器**（8×昇腾 910B，Opus 4.8 执行，无本对话上下文，一切以本文件+设计文档为准）。
- 依赖图：`B1 → B2 → B3 → B4`；`A1、A2、A4 可并行先行`；`A3 依赖 A2`；`A5 依赖 B4（模板库+schema 已合入 git）`。
- 服务器上 JoyAI 仓库与数据集路径由服务器端自查（此前 toy 方案 SFT 已在该机跑通，环境应已存在）；找不到时在实验记录中写明实际路径并继续。

## 契约 C0：数据 schema v1（双方共同遵守，改动即"大事"）

每段数据（episode）是一个目录：

```
episode_<8位id>/
  meta.json           # 必需
  timeline.json       # 必需
  actions.npy         # 必需 float32 [T, 9]，T = round(duration_s * 30)
  audio_user.wav      # 可选 预留字段（无音频原则:初期不产出,动作专家不接音频;见设计文档 §2 洞察 3）
  hidden_states.npy   # 可选 float32 [N_decisions, d_model]（A3 产出）
  frames/             # 可选 视觉帧 frame_%06d.jpg @1fps
```

`meta.json` 必需键：

```json
{
  "episode_id": "ep_00000001",
  "source": "synthetic_v1",
  "duration_s": 30.0,
  "fps_action": 30,
  "seed": 42,
  "template_events": [{"label": "nod", "t": 7.0, "params": {"amp": 0.15, "freq": 1.2, "cycles": 2}}],
  "origin_video": "XKyuguz1jHc.mp4",
  "schema_version": 1
}
```

`timeline.json` 是事件列表，时间升序：

```json
[
  {"t": 4.0, "type": "user_text",  "text": "为什么她戴着独角兽角?"},
  {"t": 7.0, "type": "response",   "text": "只是今天想感受点魔法!", "est_speech_dur_s": 2.1, "action_label": "wiggle_antennas"},
  {"t": 8.0, "type": "silence"}
]
```

- `type ∈ {user_text, response, silence, delegate}`；`silence` 每秒一条可省略（默认无事件即沉默）。
- `est_speech_dur_s` = 字符数 ÷ 语速（中文 5.5 字/秒，英文 14 字符/秒），时长代理，两侧统一用此公式。
- 数据集根目录放 `stats.json`（9 维的 min/max/mean/std，由 A5 聚合产出）。

---

# B 系列（本机）

## 任务 B1：动作空间勘察

**Files:**

- Create: `sim/probe_action_space.py`
- Create: `sim/limits.json`（脚本产出）
- Create: `docs/experiments/M1-B1-动作空间勘察报告.md`（手写结论）

**Interfaces:**

- Produces: `sim/limits.json`，格式 `{"dim_names": [...9个名字...], "safe_min": [9], "safe_max": [9], "max_vel": [9], "achieved_hz": 30.0}`。B2 的模板限幅、后续安全层、归一化都吃它。

**前置（本机固定配方，出自 reachy-mini-demo/仿真环境实现文档1.md）：**

```powershell
Get-Process python,reachy-mini-daemon -EA SilentlyContinue | Stop-Process -Force
E:\Github\ReachyMni_Project\reachy-mini-demo\.venv\Scripts\reachy-mini-daemon.exe --sim --scene minimal
# 等 REST 就绪（浏览器开 http://localhost:8000/docs），再静置 ~25 秒
```

- [x] **Step 1：写勘察脚本**

```python
# sim/probe_action_space.py —— 动作空间勘察:逐维扫限位 + 30Hz 压测
import os
os.environ.setdefault("NO_PROXY", "localhost,127.0.0.1")
import time, json
import numpy as np
from scipy.spatial.transform import Rotation as R
from reachy_mini import ReachyMini

DIMS = ["x","y","z","roll","pitch","yaw","body_yaw","ant_right","ant_left"]
DT = 1.0 / 30.0

def pose_mat(neutral, x,y,z,roll,pitch,yaw):
    d = np.eye(4); d[:3,:3] = R.from_euler("xyz",[roll,pitch,yaw]).as_matrix(); d[:3,3] = [x,y,z]
    return neutral @ d

def read_state(mini, neutral_inv):
    cur = neutral_inv @ mini.get_current_head_pose()
    xyz = cur[:3,3]; rpy = R.from_matrix(cur[:3,:3]).as_euler("xyz")
    ants = mini.get_present_antenna_joint_positions()  # 顺序假定 [右,左],与 set_target 一致;若观察相反则在报告记录并交换
    return np.array([*xyz, *rpy, 0.0, ants[0], ants[1]])  # body_yaw 读数不可靠,扫描时对第6维特殊处理

def apply(mini, neutral, a):
    mini.set_target(head=pose_mat(neutral, *a[:6]), antennas=[a[7], a[8]], body_yaw=float(a[6]))

def sweep_dim(mini, neutral, neutral_inv, i, step, max_abs, err_tol):
    """逐步加大第 i 维指令,实测跟踪误差,误差超容差即视为触顶。返回 (safe_neg, safe_pos)。"""
    bounds = []
    for sign in (+1, -1):
        v, safe = 0.0, 0.0
        while abs(v) < max_abs:
            v += sign * step
            a = np.zeros(9); a[i] = v
            for _ in range(15): apply(mini, neutral, a); time.sleep(DT)   # 0.5s 逼近
            time.sleep(0.3)
            err = abs(read_state(mini, neutral_inv)[i] - v)
            if err > err_tol: break
            safe = v
        bounds.append(safe)
        a = np.zeros(9)
        for _ in range(30): apply(mini, neutral, a); time.sleep(DT)      # 回中
    return bounds[1], bounds[0]

def rate_test(mini, neutral, seconds=30, freq=1.0, amp=0.17):
    """pitch 正弦 30Hz 流,测实际循环频率与最大周期抖动。"""
    n = int(seconds / DT); tics = []
    t0 = time.perf_counter()
    for k in range(n):
        a = np.zeros(9); a[4] = amp * np.sin(2*np.pi*freq*k*DT)
        apply(mini, neutral, a)
        tics.append(time.perf_counter())
        nxt = t0 + (k+1)*DT
        while time.perf_counter() < nxt: pass
    d = np.diff(tics)
    return {"achieved_hz": float(1/np.mean(d)), "max_jitter_ms": float(np.max(d)*1000 - DT*1000)}

def main():
    mini = ReachyMini()
    neutral = mini.get_current_head_pose()
    neutral_inv = np.linalg.inv(neutral)
    cfg = [  # (step, max_abs, err_tol) 每维扫描参数
        (0.005,0.06,0.004),(0.005,0.06,0.004),(0.005,0.06,0.004),        # xyz (m)
        (0.05,0.8,0.04),(0.05,0.8,0.04),(0.05,1.2,0.04),                 # rpy (rad)
        (0.1,3.2,0.08),(0.1,3.2,0.15),(0.1,3.2,0.15)]                    # body_yaw, ants
    mins, maxs = [], []
    for i,(s,m,e) in enumerate(cfg):
        if i == 6:  # body_yaw 无可靠实测读数:采用保守文档值,肉眼观察仿真中是否达位,A4 负责核实
            lo, hi = -2.8, 2.8
            print(f"{DIMS[i]:9s} 跳过实测,取文档保守值 [{lo:+.3f}, {hi:+.3f}](待 A4 核实)")
        else:
            lo, hi = sweep_dim(mini, neutral, neutral_inv, i, s, m, e)
            print(f"{DIMS[i]:9s} safe range: [{lo:+.3f}, {hi:+.3f}]")
        mins.append(round(lo,4)); maxs.append(round(hi,4))
    rt = rate_test(mini, neutral)
    print("30Hz 压测:", rt)
    out = {"dim_names": DIMS, "safe_min": mins, "safe_max": maxs,
           "max_vel": [0.1,0.1,0.1,2.0,2.0,2.0,2.0,6.0,6.0], **rt}   # max_vel 初值,按报告修订
    with open(os.path.join(os.path.dirname(__file__),"limits.json"),"w",encoding="utf-8") as f:
        json.dump(out, f, ensure_ascii=False, indent=2)
    print("已写出 sim/limits.json")

if __name__ == "__main__":
    main()
```

- [x] **Step 2：跑脚本**

```powershell
$env:PYTHONUTF8=1
E:\Github\ReachyMni_Project\reachy-mini-demo\.venv\Scripts\python.exe sim\probe_action_space.py
```

预期：9 行 `safe range`、一行 `30Hz 压测: {'achieved_hz': ≈30, 'max_jitter_ms': <10}`、`已写出 sim/limits.json`。若 achieved_hz 明显低于 30，把结论写进报告（后续块长/频率据此调）。

- [x] **Step 3：写勘察报告**（`docs/experiments/M1-B1-动作空间勘察报告.md`：每维安全范围表、达成频率、观察到的异常、对 max_vel 的修订值）

- [x] **Step 4：Commit**

```bash
git add sim/ docs/experiments/M1-B1-动作空间勘察报告.md
git commit -m "M1-B1: 动作空间勘察——9维安全限位实测 + 30Hz set_target 压测"
```

## 任务 B2：轨迹模板库

**Files:**

- Create: `data_gen/__init__.py`（空文件）、`data_gen/templates.py`
- Create: `conftest.py`（仓库根，空文件——让 pytest 把仓库根加进 sys.path，`data_gen` 才可导入）
- Test: `tests/test_templates.py`

**Interfaces:**

- Consumes: `sim/limits.json`（B1）。

- Produces（A5 依赖，签名不得改）：
  
  - `render_template(label: str, duration_s: float, rng: np.random.Generator, params: dict|None=None) -> tuple[np.ndarray, dict]`：返回 `(traj [T,9] float32, 实际使用的 params)`；`label ∈ {"nod","shake_head","tilt_head","wiggle_antennas","none"}`。
  - `idle_motion(duration_s: float, rng) -> np.ndarray [T,9]`
  - `backchannel_nods(duration_s: float, speech_spans: list[tuple[float,float]], rng) -> np.ndarray [T,9]`
  - `compose(duration_s: float, events: list[dict], speech_spans, rng, limits: dict) -> np.ndarray [T,9]`：idle + backchannel + 事件模板叠加后按 limits 逐维裁剪。`events` 元素 = `{"label","t","params"(可省)}`。

- [x] **Step 1：先写失败测试**

```python
# tests/test_templates.py
import numpy as np, json, pathlib
from data_gen.templates import render_template, idle_motion, backchannel_nods, compose

RNG = np.random.default_rng(0)
LIMITS = json.loads((pathlib.Path(__file__).parent.parent/"sim"/"limits.json").read_text(encoding="utf-8"))

def test_render_shapes_and_labels():
    for label in ["nod","shake_head","tilt_head","wiggle_antennas","none"]:
        traj, params = render_template(label, 3.0, RNG)
        assert traj.shape == (90, 9) and traj.dtype == np.float32

def test_nod_moves_pitch_only_mainly():
    traj, _ = render_template("nod", 3.0, RNG)
    assert np.abs(traj[:,4]).max() > 0.03          # pitch 显著(阈值留裕量:高频+强衰减的随机组合下首峰可能较小)
    assert np.abs(traj[:,5]).max() < 1e-6          # yaw 不动

def test_trajectory_starts_and_ends_at_zero():
    for label in ["nod","shake_head","tilt_head","wiggle_antennas"]:
        traj, _ = render_template(label, 4.0, RNG)
        assert np.abs(traj[0]).max() < 1e-3 and np.abs(traj[-1]).max() < 0.01

def test_compose_respects_limits_and_continuity():
    events = [{"label":"nod","t":2.0},{"label":"wiggle_antennas","t":5.0}]
    traj = compose(10.0, events, speech_spans=[(1.0,4.0)], rng=RNG, limits=LIMITS)
    assert traj.shape == (300, 9)
    lo, hi = np.array(LIMITS["safe_min"]), np.array(LIMITS["safe_max"])
    assert (traj >= lo - 1e-6).all() and (traj <= hi + 1e-6).all()
    vel = np.abs(np.diff(traj, axis=0)) * 30
    assert (vel <= np.array(LIMITS["max_vel"]) + 1e-6).all()   # 速度限内=连续
```

- [x] **Step 2：跑测试确认失败**

```powershell
E:\Github\ReachyMni_Project\reachy-mini-demo\.venv\Scripts\python.exe -m pytest tests/test_templates.py -v
```

预期：`ModuleNotFoundError: data_gen.templates`（pytest 不在 venv 时先 `pip install pytest -i https://pypi.tuna.tsinghua.edu.cn/simple`）。

- [x] **Step 3：实现模板库**

```python
# data_gen/templates.py —— 5 类标签的参数随机化连续轨迹 + idle 微动 + backchannel
import numpy as np

FPS = 30
# 维度索引: 0x 1y 2z 3roll 4pitch 5yaw 6body_yaw 7ant_r 8ant_l

def _T(duration_s): return int(round(duration_s * FPS))

def _envelope(T, attack=0.15, release=0.25):
    """起止平滑包络(smoothstep),保证轨迹从 0 起、回 0 终。"""
    t = np.linspace(0, 1, T)
    def smooth(x): return np.clip(x, 0, 1)**2 * (3 - 2*np.clip(x, 0, 1))
    return smooth(t/attack) * smooth((1-t)/release)

def _sine_burst(T, dim, amp, freq, cycles, decay):
    traj = np.zeros((T, 9), dtype=np.float32)
    t = np.arange(T) / FPS
    dur = min(cycles / freq, T / FPS)
    n = int(dur * FPS)
    if n < 2: return traj
    osc = amp * np.sin(2*np.pi*freq*t[:n]) * np.exp(-decay*t[:n]) * _envelope(n)
    traj[:n, dim] = osc
    return traj

def render_template(label, duration_s, rng, params=None):
    T = _T(duration_s); p = dict(params or {})
    def pick(k, lo, hi):
        if k not in p: p[k] = float(rng.uniform(lo, hi))
        return p[k]
    if label == "nod":
        traj = _sine_burst(T, 4, pick("amp",0.09,0.26), pick("freq",0.8,2.0),
                           pick("cycles",1,3), pick("decay",0.2,0.8))
    elif label == "shake_head":
        traj = _sine_burst(T, 5, pick("amp",0.09,0.35), pick("freq",0.7,1.6),
                           pick("cycles",2,4), pick("decay",0.1,0.5))
    elif label == "tilt_head":
        amp = pick("amp",0.13,0.30) * (1 if rng.random() < 0.5 else -1)
        hold = pick("hold_s",0.5,1.5)
        T_r = int(0.4*FPS); T_h = min(int(hold*FPS), T - 2*T_r)
        traj = np.zeros((T,9), dtype=np.float32)
        if T_h > 0:
            ramp = amp * _envelope(2*T_r, 0.5, 0.5)[:T_r] / max(_envelope(2*T_r,0.5,0.5)[:T_r].max(),1e-9)
            seg = np.concatenate([ramp, np.full(T_h, amp), ramp[::-1]])
            traj[:len(seg), 3] = seg[:T]
    elif label == "wiggle_antennas":
        a = pick("amp",0.3,0.8); f = pick("freq",2.0,4.0); c = pick("cycles",2,6)
        traj = _sine_burst(T, 7, a, f, c, 0.3) + _sine_burst(T, 8, -a, f, c, 0.3)
    elif label == "none":
        traj = np.zeros((T,9), dtype=np.float32)
    else:
        raise ValueError(f"未知标签: {label}")
    return traj.astype(np.float32), p

def idle_motion(duration_s, rng):
    """低幅多正弦漂移 + 类呼吸,贯穿全程的'活着感'。"""
    T = _T(duration_s); t = np.arange(T)/FPS
    traj = np.zeros((T,9), dtype=np.float32)
    traj[:,2] = 0.003 * np.sin(2*np.pi*0.25*t + rng.uniform(0,6.28))          # z 呼吸 ±3mm
    for dim, amp in [(3,0.015),(4,0.015),(5,0.02)]:                            # rpy 慢漂
        for f in (0.07, 0.13, 0.23):
            traj[:,dim] += amp/3 * np.sin(2*np.pi*f*t + rng.uniform(0,6.28))
    env = _envelope(T, attack=0.05, release=0.05)
    return (traj * env[:,None]).astype(np.float32)

def backchannel_nods(duration_s, speech_spans, rng):
    """用户说话区间内,每 2~5 秒随机插入一次小幅点头。"""
    T = _T(duration_s)
    traj = np.zeros((T,9), dtype=np.float32)
    for (s, e) in speech_spans:
        t = s + rng.uniform(0.5, 2.0)
        while t < e - 1.0:
            nod, _ = render_template("nod", 1.5, rng, {"amp": rng.uniform(0.05,0.10)})
            i = int(t*FPS)
            n = min(len(nod), T - i)
            traj[i:i+n] += nod[:n]
            t += rng.uniform(2.0, 5.0)
    return traj

def compose(duration_s, events, speech_spans, rng, limits):
    T = _T(duration_s)
    traj = idle_motion(duration_s, rng) + backchannel_nods(duration_s, speech_spans or [], rng)
    for ev in events:
        seg, _ = render_template(ev["label"], min(4.0, duration_s - ev["t"]), rng, ev.get("params"))
        jitter = rng.uniform(-0.2, 0.5)                                        # 时间锚定抖动
        i = max(0, int((ev["t"] + jitter) * FPS))
        n = min(len(seg), T - i)
        if n > 0: traj[i:i+n] += seg[:n]
    lo = np.array(limits["safe_min"], dtype=np.float32)
    hi = np.array(limits["safe_max"], dtype=np.float32)
    traj = np.clip(traj, lo, hi)
    mv = np.array(limits["max_vel"], dtype=np.float32) / FPS                   # 每步最大位移
    for k in range(1, len(traj)):                                              # 逐步限速=保证连续
        traj[k] = np.clip(traj[k], traj[k-1]-mv, traj[k-1]+mv)
    return traj.astype(np.float32)
```

- [x] **Step 4：跑测试确认通过**（同 Step 2 命令，预期 4 passed）

- [x] **Step 5：Commit**

```bash
git add data_gen/ tests/test_templates.py
git commit -m "M1-B2: 轨迹模板库——5类标签+idle微动+backchannel,参数随机化,限幅限速"
```

## 任务 B3：schema 校验器 + 仿真回放器

**Files:**

- Create: `data_gen/schema.py`、`sim/replay_episode.py`
- Test: `tests/test_schema.py`

**Interfaces:**

- Produces（A5/A6 依赖）：`validate_episode(path: str|Path) -> list[str]`（错误列表，空=合法）；`write_episode(path, meta: dict, timeline: list, actions: np.ndarray) -> None`。

- Produces：`sim/replay_episode.py <episode_dir>` 命令行回放。

- [x] **Step 1：先写失败测试**

```python
# tests/test_schema.py
import numpy as np
from data_gen.schema import write_episode, validate_episode

META = {"episode_id":"ep_test0001","source":"synthetic_v1","duration_s":2.0,
        "fps_action":30,"seed":1,"template_events":[],"origin_video":"x.mp4","schema_version":1}

def test_roundtrip_valid(tmp_path):
    ep = tmp_path/"ep_test0001"
    write_episode(ep, META, [{"t":1.0,"type":"response","text":"好的",
                              "est_speech_dur_s":0.4,"action_label":"nod"}],
                  np.zeros((60,9), dtype=np.float32))
    assert validate_episode(ep) == []

def test_detects_bad_shape(tmp_path):
    ep = tmp_path/"ep_test0002"
    write_episode(ep, {**META,"episode_id":"ep_test0002"}, [], np.zeros((59,9), dtype=np.float32))
    errs = validate_episode(ep)
    assert any("T" in e or "帧数" in e for e in errs)

def test_detects_nan(tmp_path):
    ep = tmp_path/"ep_test0003"
    a = np.zeros((60,9), dtype=np.float32); a[5,3] = np.nan
    write_episode(ep, {**META,"episode_id":"ep_test0003"}, [], a)
    assert any("NaN" in e for e in validate_episode(ep))
```

- [x] **Step 2：跑测试确认失败**（`ModuleNotFoundError: data_gen.schema`）

- [x] **Step 3：实现**

```python
# data_gen/schema.py —— episode 读写与校验(契约 C0 的代码化)
import json
import numpy as np
from pathlib import Path

REQUIRED_META = ["episode_id","source","duration_s","fps_action","seed",
                 "template_events","origin_video","schema_version"]
EVENT_TYPES = {"user_text","response","silence","delegate"}

def write_episode(path, meta, timeline, actions):
    p = Path(path); p.mkdir(parents=True, exist_ok=True)
    (p/"meta.json").write_text(json.dumps(meta, ensure_ascii=False, indent=1), encoding="utf-8")
    (p/"timeline.json").write_text(json.dumps(timeline, ensure_ascii=False, indent=1), encoding="utf-8")
    np.save(p/"actions.npy", actions.astype(np.float32))

def validate_episode(path):
    p = Path(path); errs = []
    for f in ("meta.json","timeline.json","actions.npy"):
        if not (p/f).exists(): errs.append(f"缺文件 {f}")
    if errs: return errs
    meta = json.loads((p/"meta.json").read_text(encoding="utf-8"))
    for k in REQUIRED_META:
        if k not in meta: errs.append(f"meta 缺键 {k}")
    a = np.load(p/"actions.npy")
    if a.ndim != 2 or a.shape[1] != 9: errs.append(f"actions 形状 {a.shape} ≠ [T,9]")
    if a.dtype != np.float32: errs.append(f"actions dtype {a.dtype} ≠ float32")
    if np.isnan(a).any(): errs.append("actions 含 NaN")
    if "duration_s" in meta and "fps_action" in meta:
        T = int(round(meta["duration_s"] * meta["fps_action"]))
        if len(a) != T: errs.append(f"帧数 T={len(a)} 与 duration*fps={T} 不符")
    tl = json.loads((p/"timeline.json").read_text(encoding="utf-8"))
    last = -1.0
    for ev in tl:
        if ev.get("type") not in EVENT_TYPES: errs.append(f"非法事件类型 {ev.get('type')}")
        if ev.get("t", 0) < last: errs.append("timeline 未按时间升序")
        last = ev.get("t", last)
        if ev.get("type") == "response" and "est_speech_dur_s" not in ev:
            errs.append(f"response@{ev.get('t')} 缺 est_speech_dur_s")
    return errs
```

```python
# sim/replay_episode.py —— 把 episode 的动作轨迹以 30Hz 回放到 MuJoCo 仿真
# 用法: python sim/replay_episode.py <episode_dir>
import os, sys, time, json
os.environ.setdefault("NO_PROXY", "localhost,127.0.0.1")
import numpy as np
from pathlib import Path
from scipy.spatial.transform import Rotation as R
from reachy_mini import ReachyMini

def main(ep_dir):
    p = Path(ep_dir)
    actions = np.load(p/"actions.npy")
    meta = json.loads((p/"meta.json").read_text(encoding="utf-8"))
    mini = ReachyMini()
    neutral = mini.get_current_head_pose()
    print(f"回放 {meta['episode_id']}: {len(actions)} 帧 @30Hz")
    t0 = time.perf_counter()
    for k, a in enumerate(actions):
        d = np.eye(4)
        d[:3,:3] = R.from_euler("xyz", a[3:6]).as_matrix(); d[:3,3] = a[:3]
        mini.set_target(head=neutral @ d, antennas=[float(a[7]), float(a[8])],
                        body_yaw=float(a[6]))
        nxt = t0 + (k+1)/30
        while time.perf_counter() < nxt: pass
    print(f"完成,实际用时 {time.perf_counter()-t0:.1f}s (标称 {len(actions)/30:.1f}s)")

if __name__ == "__main__":
    main(sys.argv[1])
```

- [x] **Step 4：跑测试确认通过**（3 passed）
- [x] **Step 5：起 sim daemon，肉眼回放一段**：先用 B2 模板生成一段临时 episode（下个任务会正式做），或直接跳过留到 B4。 —— **跳过，留到 B4 统一抽查回放（计划允许）**
- [x] **Step 6：Commit**

```bash
git add data_gen/schema.py sim/replay_episode.py tests/test_schema.py
git commit -m "M1-B3: episode 读写/校验器 + MuJoCo 30Hz 回放器"
```

## 任务 B4：端到端样例——100 段样例数据 + 抽查回放

**Files:**

- Create: `data_gen/make_samples.py`
- Create: `samples/`（产出，入库供服务器对接测试；正式数据不入库，样例入库）

**Interfaces:**

- Consumes: B2 `compose`、B3 `write_episode/validate_episode`。

- Produces: `samples/ep_*/`（100 段合法 episode，服务器 A5 的对接测试样本）。

- [x] **Step 1：写生成脚本**

```python
# data_gen/make_samples.py —— 用模板库+schema 生成 100 段合成样例(无音频/无帧)
import json, pathlib
import numpy as np
from data_gen.templates import compose
from data_gen.schema import write_episode, validate_episode

ROOT = pathlib.Path(__file__).parent.parent
LIMITS = json.loads((ROOT/"sim"/"limits.json").read_text(encoding="utf-8"))
LABELS = ["nod","shake_head","tilt_head","wiggle_antennas","none"]
UTTER = ["好的,没问题!","这个我不太同意。","让我想想…","哈哈,有意思!","你说的是那个红色的吗?"]

def est_dur(text): return round(len(text) / 5.5, 2)   # 中文时长代理:5.5字/秒

def main(n=100):
    rng = np.random.default_rng(2026)
    ok = 0
    for i in range(n):
        dur = float(rng.uniform(15, 40))
        t_resp = sorted(rng.uniform(3, dur-4, size=rng.integers(1, 4)))
        events, timeline, spans = [], [], []
        for t in t_resp:
            t = float(round(t, 1))
            label = LABELS[rng.integers(0, len(LABELS))]
            text = UTTER[rng.integers(0, len(UTTER))]
            q_t = round(max(0.5, t - rng.uniform(1.5, 3.0)), 1)
            timeline.append({"t": q_t, "type": "user_text", "text": "(用户提问)"})
            spans.append((q_t, q_t + 1.5))
            timeline.append({"t": t, "type": "response", "text": text,
                             "est_speech_dur_s": est_dur(text), "action_label": label})
            if label != "none": events.append({"label": label, "t": t})
        traj = compose(dur, events, spans, rng, LIMITS)
        meta = {"episode_id": f"ep_{i:08d}", "source": "synthetic_v1",
                "duration_s": round(dur, 3), "fps_action": 30, "seed": 2026,
                "template_events": events, "origin_video": "sample", "schema_version": 1}
        ep = ROOT/"samples"/meta["episode_id"]
        write_episode(ep, meta, sorted(timeline, key=lambda e: e["t"]), traj)
        errs = validate_episode(ep)
        assert not errs, f"{ep}: {errs}"
        ok += 1
    print(f"生成并校验通过 {ok}/{n} 段 → samples/")

if __name__ == "__main__":
    main()
```

- [x] **Step 2：运行**（必须在仓库根、用 `-m` 方式，否则 `data_gen` 导入失败）：

```powershell
$env:PYTHONUTF8=1
E:\Github\ReachyMni_Project\reachy-mini-demo\.venv\Scripts\python.exe -m data_gen.make_samples
```

预期：`生成并校验通过 100/100 段 → samples/`

- [x] **Step 3：起 sim daemon，抽查回放 3 段**（`python sim\replay_episode.py samples\ep_00000000` 等），肉眼确认：有呼吸感、事件处动作明显、无跳变、无撞限位。观感问题记进 `docs/experiments/M1-B4-样例观感记录.md`（模板参数的调整会成为常态，记录基线很重要）。
- [x] **Step 4：Commit**

```bash
git add data_gen/make_samples.py samples/ docs/experiments/M1-B4-样例观感记录.md
git commit -m "M1-B4: 100段端到端样例数据+回放抽查——服务器对接基准"
```

---

# A 系列（服务器，Opus 4.8 执行）

> 开工前先读 `docs/2026-07-14-交互VLA设计文档_zh.md` 全文。以下任务可按依赖图并行。

## 任务 A1：NPU 冒烟测试三件套

**Files:**

- Create: `smoke/npu_basic.py`、`smoke/npu_dit.py`、`smoke/npu_starvla.md`（StarVLA 是装+跑，产出笔记）
- Create: `docs/experiments/M1-A1-NPU冒烟报告.md`

**Interfaces:**

- Produces: 报告中明确三个结论：基础算子 OK?/DiT 反传 OK?/StarVLA 可 import 及其流匹配头可前向? 每条 OK/FAIL+绕行方案。

- [x] **Step 1：基础算子冒烟**

```python
# smoke/npu_basic.py —— torch_npu 基础与融合注意力可用性
import torch, torch_npu  # noqa
dev = "npu:0"
x = torch.randn(4, 8, 128, 64, device=dev, dtype=torch.float16)
try:
    out = torch_npu.npu_fusion_attention(x, x, x, head_num=8, input_layout="BNSD")[0]
    print("npu_fusion_attention: OK", out.shape)
except Exception as e:
    print("npu_fusion_attention: FAIL ->", e, "(回退朴素 attention,记录性能差距)")
y = torch.nn.functional.scaled_dot_product_attention(x, x, x)
print("sdpa: OK", y.shape, "| device:", torch.npu.get_device_name(0))
```

- [x] **Step 2：DiT 小模型训练步冒烟**

```python
# smoke/npu_dit.py —— 一个 6 层 DiT 风格块在 NPU 上的前向+反传+优化器 step
import torch, torch_npu, time  # noqa
dev = "npu:0"

class Block(torch.nn.Module):
    def __init__(s, d=384, h=6):
        super().__init__()
        s.attn = torch.nn.MultiheadAttention(d, h, batch_first=True)
        s.mlp = torch.nn.Sequential(torch.nn.Linear(d, 4*d), torch.nn.GELU(), torch.nn.Linear(4*d, d))
        s.n1, s.n2 = torch.nn.LayerNorm(d), torch.nn.LayerNorm(d)
        s.ada = torch.nn.Linear(d, 2*d)   # AdaLN 风格条件注入(简化)
    def forward(s, x, c):
        g, b = s.ada(c).chunk(2, -1)
        h = s.n1(x) * (1 + g[:, None]) + b[:, None]
        x = x + s.attn(h, h, h, need_weights=False)[0]
        return x + s.mlp(s.n2(x))

net = torch.nn.ModuleList([Block() for _ in range(6)]).to(dev)
opt = torch.optim.AdamW(net.parameters(), lr=1e-4)
x = torch.randn(8, 45, 384, device=dev)      # 45 步动作块 token
c = torch.randn(8, 384, device=dev)          # 条件向量
t0 = time.time()
for i in range(20):
    h = x
    for blk in net: h = blk(h, c)
    loss = h.pow(2).mean()
    opt.zero_grad(); loss.backward(); opt.step()
print(f"DiT 冒烟: 20 步 OK, {(time.time()-t0)/20*1000:.1f} ms/step, loss={loss.item():.4f}")
```

- [x] **Step 3：StarVLA 装与摸**

```bash
git clone https://github.com/starVLA/starVLA && cd starVLA
pip install -e . -i https://pypi.tuna.tsinghua.edu.cn/simple
python -c "import importlib,pkgutil,starVLA; print([m.name for m in pkgutil.iter_modules(starVLA.__path__)])"
```

然后找到其流匹配/动作头模块（按其 README 的模块索引），写 10 行脚本构造随机输入跑一次前向；CUDA 硬编码处记录到笔记（这些是我们抄代码时要改的点）。

- [x] **Step 4：写报告 + Commit**

```bash
git add smoke/ docs/experiments/M1-A1-NPU冒烟报告.md
git commit -m "M1-A1: NPU 冒烟——基础算子/DiT 反传/StarVLA 摸底"
```

## 任务 A2：JoyAI transformers 慢路径推理跑通

**Files:**

- Create: `expert/hidden_export/run_joyai.py`
- Create: `docs/experiments/M1-A2-JoyAI慢路径报告.md`

**Interfaces:**

- Produces（A3 依赖）：`load_joyai() -> (model, processor)` 与 `build_second_by_second_messages(timeline, frames) -> list[dict]`（严格按 JoyAI datasets/README 的每秒 user/assistant 格式）。

**步骤要点：**

- [x] **Step 1**：定位服务器上 JoyAI 权重与代码（toy SFT 用过，环境在）；加载方式**抄 JoyAI 仓库 `services/webinfer` 的模型加载代码**（那是官方用法，别自己发明）；若 webinfer 走 vLLM，则用其模型名+`transformers.AutoModelForCausalLM/AutoProcessor(trust_remote_code=True)` 重建慢路径。
- [x] **Step 2**：按 `datasets/README.md` 的转换格式构造一段 10 秒的每秒对话（`<t seconds>\n<image>` + `</silence>`/`</response>`），用数据集里任一真实样本的帧。
- [x] **Step 3**：`model.generate(max_new_tokens=32)` 跑最后一秒，打印输出。预期：以 `</silence>` 或 `</response>` 开头。
- [x] **Step 4**：测单卡吞吐（决策/秒），写报告 + Commit：

```bash
git add expert/hidden_export/run_joyai.py docs/experiments/M1-A2-JoyAI慢路径报告.md
git commit -m "M1-A2: JoyAI transformers 慢路径推理跑通+吞吐测量"
```

## 任务 A3：隐状态离线预计算工具（⚠️ 一周判定点）

**Files:**

- Create: `expert/hidden_export/precompute.py`
- Create: `docs/experiments/M1-A3-隐状态预计算报告.md`

**Interfaces:**

- Consumes: A2 的 `load_joyai/build_second_by_second_messages`；B3 schema（episode 目录）。
- Produces: `precompute_episode(model, processor, ep_dir) -> None`，写 `<ep_dir>/hidden_states.npy [N_decisions, d_model] float32` + meta.json 增写 `"d_model": <int>`。

**核心实现要求（判定"通"的标准）：**

- [x] **Step 1**：对每个决策秒，前向到该秒 assistant 起始位置，取 **last-layer 在决策 token（`</silence>`/`</response>` 首 token）处的 hidden state**。transformers 路径：`model(..., output_hidden_states=True)`，`hidden_states[-1][0, pos, :]`；一次前向可并取全部秒的位置（teacher forcing 整段跑一遍，按 assistant 起始位置索引切片——**不需要逐秒 generate**）。
- [x] **Step 2**：在 B4 的 `samples/` 任取 10 段跑通（样例无帧则用纯文本+黑帧占位，报告中声明）；校验 `hidden_states.npy` 形状 = timeline 决策秒数 × d_model、无 NaN。
- [x] **Step 3**：测吞吐（段/分钟·卡），估算 10 万段全量预计算的卡时，写报告。
- [x] **判定**：开工 5 个工作日内 Step 2 未通 → 停止此路线，在报告写明卡点，启动设计文档 §3.3 的**文本 Plan B**（通知本地，本地会出 Plan B 补充计划）；已通 → 全量预计算排到 A5 数据就绪后台跑。
- [x] **Commit**：`git commit -m "M1-A3: 隐状态离线预计算工具+吞吐评估"`

## 任务 A4：SDK/电机适配研究（纯调研，产出笔记）

**Files:**

- Create: `docs/experiments/M1-A4-SDK电机适配笔记.md`

**做法：** 服务器 `pip install "reachy-mini[mujoco]" -i https://pypi.tuna.tsinghua.edu.cn/simple`（Python 3.12），headless 跑仿真（`MUJOCO_GL=egl` 或 `osmesa`）；结合官方开发者文档 https://reachymini.net/zh/developers.html 与 SDK 源码，笔记必须回答：

- [ ] ① `set_target` 到电机的完整链路（REST/WebSocket 协议、daemon 内插值行为、有无内置限速）；② 控制频率上限与超频行为（丢弃/排队?）；③ 9 维各自的硬限位（与本机 B1 实测对照）；④ 电机型号/减速比/是否可整机断力矩手掰（关系 M3 示教）；⑤ headless MuJoCo 在服务器可用否（关系后续自动化评测在哪跑）；⑥ Stewart 平台 body_yaw 补偿的准确语义。
- [x] **Commit**：`git commit -m "M1-A4: SDK/电机适配研究笔记"`

## 任务 A5：标签→轨迹展开流水线（规模化数据集 v1）

**Files:**

- Create: `data_gen/pipeline.py`、`data_gen/stats.py`
- Test: `tests/test_pipeline.py`
- Create: `docs/experiments/M1-A5-数据集v1报告.md`

**Interfaces:**

- Consumes: B2 `compose`（签名见任务 B2）、B3 `write_episode/validate_episode`、JoyAI 标注数据（`datasets/example.json` 同构：`question[].{content,time}`、`response[][].{content,time,action}`）。
- Produces: 数据集 v1（≥10 万段，存服务器本地盘，不入 git）+ 数据集根 `stats.json`（9 维 min/max/mean/std + 段数/总时长）。

**步骤要点：**

- [x] **Step 1（TDD）**：先写 `tests/test_pipeline.py`——用 `datasets/example.json` 的第一个样本调 `convert_sample(sample, rng, limits) -> (meta, timeline, actions)`，断言：timeline 含全部 question（`user_text`）与 response（含 `est_speech_dur_s` 与 `action_label`）、actions 形状 = 视频秒数×30、`validate_episode` 通过。
- [x] **Step 2**：实现 `convert_sample`：user_text 时刻 → speech_spans（跨度 = `est_dur(content)`，同一公式 5.5 字/秒中文、14 字符/秒英文）；带 action 标签的 response → events；`compose()` 生成轨迹；duration 取最后事件 + 5s 与视频长的较小者，上限 120s。
- [x] **Step 3**：批量驱动：multiprocessing 扫标注文件目录（增量：已存在且校验通过的 episode 跳过），逐段 `validate_episode`，失败进隔离目录并计数。
- [ ] **Step 4**：跑 1000 段试产 → 抽查 → 全量 ≥10 万段；`data_gen/stats.py` 聚合 `stats.json`。
  🟡 **阻塞：JoyAI 真标注数据未到位（`datasets/` 空）**。代码与小样已通（3 段端到端跑通），
  真数据落盘即可全量。见 `docs/experiments/M1-A5-数据集v1报告.md`。
- [ ] **Step 5**：报告（段数、时长分布、标签分布、失败率与原因 top3）+ Commit（代码与报告入库，数据不入库）。
  🟡 代码/契约/小样报告已入库；全量数字待真数据。

## ~~任务 A6：用户音频合成~~（已取消，2026-07-14 v1.2 修订）

设计文档确立**无音频原则**（§2 洞察 3）：动作专家初期不接任何音频通道，前期实验纯文本进出。本任务整体取消；schema 中 `audio_user.wav` 仅作预留字段。若未来解除无音频原则，另出补充计划。

---

## M1 出口检查单（对照设计文档 §6）

- [x] `sim/limits.json` 实测产出，30Hz 达成率有结论（B1）
- [x] 模板库+schema+回放器测试全绿（B2/B3）
- [x] 100 段样例入库、肉眼回放合格（B4）
- [x] NPU 三件套冒烟结论明确（A1）
- [x] JoyAI 慢路径推理+吞吐数据（A2）
- [x] 隐状态预计算通/不通判定完成；简化版通过（A3）
- [x] SDK 六问全答（A4）
- [ ] 数据集 v1 ≥10 万段 + stats.json（A5，待数据）
- [x] `docs/experiments/` 每任务一篇中文记录，全部 push
