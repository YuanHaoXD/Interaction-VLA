# M1.5：官方动作库接入与 50Hz 迁移（2026-07-15 → 07 月底）

> **For agentic workers:** 按任务逐项执行，checkbox 跟踪。执行纪律同 M1 计划与仓库 CLAUDE.md。
> 背景：设计文档 v1.3 两项修订——①动作采样率 30Hz→**50Hz**；②数据第 1 层以**官方动作库**（LIB-01，`data_gen/motion_library/`，104 动作+描述）为核心动作词汇。本计划完成迁移与接入，之后 A5 全量才允许开跑。

## 契约 C0 修正案（v1.3，随本计划生效）

- `fps_action` 规范值 **30 → 50**；`actions.npy` 的 `T = round(duration_s * 50)`。schema 校验器本就按 meta 里的 fps 校验，代码无需改；**所有生产端**（templates/make_samples/pipeline/回放）统一切 50。
- `timeline.json` 事件的 `action_label` 值域扩为：`{官方库 85 情绪动作名} ∪ {nod, shake_head, tilt_head, wiggle_antennas} ∪ {none}`。库动作在 events 里写作 `{"label": "lib:<动作名>", "t": ...}`。
- 其余不变。此修正案已获用户与本地裁决（2026-07-15）。

## 依赖图

`C1 → C2 → C4(代码改造)`；`C3 独立先行`；`C4 全量生产 = 等 C2+C3 完成`。

---

## 任务 C1（本机）：50Hz 迁移

**Files:** Modify: `data_gen/templates.py`（FPS=50）、`data_gen/make_samples.py`（fps_action=50）、`sim/replay_episode.py`（按 meta 的 fps_action 定步长，去掉硬编码 30）、`tests/test_templates.py`（形状断言 3.0s→150 步等）、`CLAUDE.md`（30Hz/45 步→50Hz/75 步；顺带修正"precompute 是简化版"的过时描述——A3 已是精确版）；Rerun: `sim/probe_action_space.py` 的 rate_test 以 DT=1/50 复测。

- [x] 改 `templates.py` 顶部 `FPS = 50`；同步 `tests/test_templates.py` 全部形状/速度断言（速度断言里的 `*30` → `*FPS`，从模块导入）
- [x] 改 `make_samples.py`：`fps_action: 50`、`est_dur` 不变；**同 seed 重新生成 100 段**（timeline 与旧版一致 → 3 段隐状态 fixture 仍有效，无需重算；报告里声明）
- [x] 改 `replay_episode.py`：`fps = meta["fps_action"]`，步长 `1/fps`
- [x] 50Hz 压测：复用 rate_test（DT=1/50，pitch 正弦 30s），把 `achieved_hz/max_jitter_ms` 更新进 `limits.json`，结论写进 `docs/experiments/M1.5-C1-50Hz迁移报告.md`。验收线：achieved ≈50Hz、抖动 <10ms；不达标则记录实测值并评估降回 30 或异步发送
- [x] 全部 pytest 绿 + 抽 1 段 50Hz 回放肉眼确认更顺滑 → commit

## 任务 C2（本机）：官方库增广接入

**Files:** Create: `data_gen/augment.py`、`tests/test_augment.py`、`sim/limits_real.json`；Modify: `data_gen/templates.py` 的 `compose()`（识别 `lib:` 事件）。

**Interfaces（C4 依赖，签名不得改）:**
- `load_lib_move(name: str) -> np.ndarray[T,9]`（从 motion_library 读 .npz 的 action9 视图，50Hz 原生）
- `augment_lib_move(traj, rng, time_scale=(0.85,1.15), amp_scale=(0.7,1.15)) -> np.ndarray[T',9]`
- `compose()` 对 `label.startswith("lib:")` 的事件走库路径，其余走原模板路径；限幅统一改用 `limits_real.json`

- [x] **库包络提取**：扫 104 个 .npz 的 action9，取逐维 min/max 加 10% 裕量 → `sim/limits_real.json`（官方动作录制于真机 → 库包络即"真机可行域"的数据驱动定义，替代仿真虚高限位；解决 B1 报告 §4.2 的仿真/真机 3–4 倍差异隐患）
- [x] 实现 `augment.py`：时间缩放=线性重采样到 `round(T*k)` 步；幅度缩放=对 9 维乘系数（天线/身体与头部可用不同系数范围）；**边界连续化**：首尾各 0.2s 乘 smoothstep 包络压到 0（官方动作起止未必在中立位，不压会与 idle 叠加跳变）
- [x] TDD：增广后 ①形状/类型合规 ②首尾近零（<0.01）③过 `limits_real` 限幅 ④与原轨迹相关且不同（0.5<相关<0.999）
- [x] `compose()` 接 `lib:` 路径 + 生成 20 段带库动作样例（events 混用库动作与模板）→ 仿真回放肉眼验收（重点：库动作与 idle 叠加处无跳变）→ 报告 + commit

## 任务 C3（⚠️ 改由本机执行，2026-07-15 改派）：标注任务 v2

> 改派原因：标注只调 API 不需要卡；且数据安全考虑——标注原料与产物留在本机，避免服务器数据出不来的问题。服务器**不做 C3**，只做 C4。

**Files:** 旧流水线代码已整体迁入本仓库 `annotation/`（`annotate_actions_api.py` + `local_api_logger/` + 原 README/CLAUDE.md，API 密钥在 `annotation/.env`，不入库）；标注原料 = `E:\Github\ReachyMni_Project\action-annotation\JoyAI-VL-Interaction\`（5GB，background/chat/event_grounding/narration 分片，留原地按绝对路径引用）。产出格式沿用 A5 契约（`data_gen/example_annotation.json`），`action` 字段值域按 C0 修正案。

- [ ] 先读 `annotation/README.md` 与 `annotation/CLAUDE.md`（旧流水线用法、"一步"平台调用方式、思考 token 的坑）
- [x] ~~新 prompt（85 选 1 + none）~~ → 试标暴露塌陷与成本问题，**已裁定改为簇级 demeanor 方案**：13 神态簇 + 簇内加权采样 + 批处理 + 动作密度控制，准则表与契约修正见 `docs/decisions/2026-07-16-C3标注裁定-簇级demeanor与候选裁剪.md`（后续步骤以裁定 §5 为准）
- [x] 试产 1 万条 → 标签分布统计+50 条随机抽样（含上下文）写 `docs/experiments/M1.5-C3-1万条试产报告.md` 并 push，**等本地审查（用户+Fable 5）通过再全量**（2026-07-17 完成：9745 条、$3.39、13簇全覆盖、健康度满分；⏸ 等把关）
- [ ] 全量标注；产物存本机（`annotation/annotated/`，不入 git），完成后**只把最终标注文件上传服务器**供 C4 消费（路径写进实验记录）

## 任务 C4（服务器）：A5 v2 全量生产

- [ ] `pipeline.convert_sample`：`action` 为库动作名时生成 `lib:` 事件（none/旧 5 类逻辑不变）；同步 fixture 与测试
- [ ] 前置自检：按 A2 计划原 Step 2/3 用真实样本+帧验证 `model.generate` 输出以 `</silence>`/`</response>` 开头（若前次复核已含此项，引用报告即可跳过）
- [ ] 等 C2+C3 就绪：全量 ≥10 万段 @50Hz（限幅用 `limits_real.json`）→ `stats.json` → 报告
- [ ] 隐状态全量预计算（8 卡，预估约 40 分钟）→ 报告（含带帧/无帧口径声明）

## 出口检查单

- [x] 全仓测试绿，50Hz 压测达标（C1）
- [x] `limits_real.json` 产出；20 段库动作样例肉眼合格（C2）
- [ ] 标注 v2 试产通过本地审查，全量完成（C3）
- [ ] 数据集 v1（≥10 万段 @50Hz）+ stats + 全量隐状态（C4）
- [ ] M2（动作专家 v1 训练）的全部输入就绪
