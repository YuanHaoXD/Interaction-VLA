# M1-A 系列：服务器重启导致缓存丢失 · 情况说明与复核记录

**日期**：2026-07-15
**执行方**：服务器侧 Opus 4.8（A 系列）
**性质**：情况说明 + 复核记录（供本地 Fable 5 审阅）
**一句话**：**服务器曾莫名重启，`/cache` 易失盘上的一切丢失；但 git 记录此前已 push，
所以仓库里留着"M1-A 系列已完成"的报告与勾选，运行时环境却是空的。本次已逐项在真硬件复核并修好。**

---

## 1. 发生了什么（用户 2026-07-15 确认）

- 上一轮 M1-A 系列（A1–A4）**确实做完过**，实验报告 + 计划勾选 + 代码都 commit 并 push 到了 GitHub。
- 之后**服务器莫名其妙重启**。本机的**易失盘（`/`、`/tmp`、`/cache`，重启即丢，共约 50GB）**上的
  内容全部丢失，包括：
  - conda 环境里对 site-packages 打的 torch_npu 修复补丁；
  - `/cache/model/JoyAI-VL-Interaction-Preview` 的 17G 权重；
  - pip 装过的 reachy-mini / StarVLA 等；
  - `/cache` 下的各种中间产物。
- **唯一永久区是 `/home/ma-user/work`（11TB）**，git 仓库在此，所以**报告与代码没丢**——
  但报告描述的"运行时已跑通"在新环境里**并不成立**（补丁没了、包没了、`/cache` 权重没了）。

**结论**：不是谁记错了，是"记录持久化了、运行环境没持久化"。本次进场先把这层矛盾查清，
再逐项重跑复核，而不是盲信旧报告的 "✅ PASS"。

## 2. 现在的资产落位（永久盘，重启不丢）

| 资产 | 路径 | 状态 |
| --- | --- | --- |
| 代码仓库 | `/home/ma-user/work/dataset/yh222/Interaction-VLA` | ✅ 在 |
| JoyAI 权重 | `/home/ma-user/work/dataset/yh222/models/JoyAI-VL-Interaction-Preview` | ✅ 已迁到永久盘（17G，Qwen3-VL，d_model=4096，36 层） |
| 标注数据集 | `/home/ma-user/work/dataset/yh222/datasets/` | ⏳ 用户将下载到此（当前空） |
| StarVLA 零件库 | `/home/ma-user/work/dataset/yh222/tools/starVLA` | ✅ 已 clone |

> 教训：**易失盘（`/cache`）只能放临时中间产物**；权重/数据集/代码必须在 `/home/ma-user/work`。
> 旧 A2 报告写的 `/cache/model/...` 正是这次丢失的根源，`run_joyai.locate_joyai()` 已把永久盘路径置顶。

## 3. 本次复核做了什么（逐项在真硬件重跑）

| 任务 | 复核结论 | 相比旧报告的修正 |
| --- | --- | --- |
| **A1** NPU 冒烟 | ✅ 真通过 | torch_npu 崩因（triton `AttrsDescriptor`）修复**脚本化** `smoke/setup_npu_env.sh`（旧补丁未持久化，这次杜绝重复踩坑）；StarVLA DiT 头 NPU **真前向** `(4,45,9)` 通过（旧版只 import 没跑前向） |
| **A2** JoyAI 慢路径 | ✅ 真通过 | 权重路径改永久盘；transformers→5.13.1；吞吐复核 **5.99 决策/秒**（旧报告 0.67 是未 warmup 误测） |
| **A3** 隐状态预计算（头号判定点）| ✅ **真正通过** | **弃旧版 `np.linspace` 占位**，改按决策 token id（151669/151670）**精确定位**；10/10 无 NaN；**310 段/分·卡 → 全量 8 卡约 40 分钟**（旧报告误估 3 天）。语义主通道锁定隐状态，**不启用文本 Plan B** |
| **A4** SDK 调研 | ✅ 升级 | 服务器**装上** reachy_mini 直读源码（绕过 cairo），六问从"推断"改**源码实据**；headless MuJoCo 实测：物理 `mj_step` 可用、离屏渲染 EGL/OSMesa 起不来 |
| **A5** 数据流水线 | 🟡 代码完成 | `convert_sample`/`pipeline`/`stats` + 测试全绿，小样端到端通过；**全量阻塞于真标注数据未到位** |

各任务详见对应 `docs/experiments/M1-A*.md`（均已按本次复核更新）。

## 4. 当前阻塞与待决（请本地 Fable 5 裁决/协调）

### 阻塞
1. **GitHub push 不通**：本机 `~/.git-credentials` 只有华为内网 codehub 的 token，**没有 github.com 凭据**。
   本次 5 个复核 commit **只在本地**（`ec5ccff / 54192d7 / b3f0888 / c085f24 / 3730e4b`，均领先 origin/main），
   **推不上去**。需用户提供 GitHub 凭据后才能同步给本地。
2. **A5 全量待数据**：JoyAI 动作标注数据（action-annotation 300 万样本）当前不在本机。
   用户将下载到 `/home/ma-user/work/dataset/yh222/datasets/`。数据到位后 `run_pipeline` 直接全量，
   随后触发 A3 全量隐状态预计算（8 卡约 40 分钟）。

### 待决（A4 复核新发现，不阻塞 M1，但影响 M2/M3）
1. **渲染类评测落点**：服务器离屏渲染（EGL/OSMesa）起不来，M2/M3 的 MuJoCo→视频→VLM 裁判
   要么运维补 GPU EGL/软渲染库，要么放本机渲染。**请定落点。**
2. **body_yaw 执行语义一致性**：数据里的 `body_yaw` 与 SDK `automatic_body_yaw=True` 的自动微调可能对不上
   （SDK 用 `inverse_kinematics_safe` 会再改 body_yaw 保机械限位）。建议训练/回放统一
   `automatic_body_yaw=False`（透传我们给的值），否则 FK 对不上。**涉及动作执行语义，先记不擅改 schema。**

## 5. 给运维/用户的两条硬约定（防重演）
- **凡是要长期存在的（权重、数据集、缓存产物），一律放 `/home/ma-user/work`**；`/cache`、`/tmp` 只当临时。
- **环境重置后的第一件事**：`cd Interaction-VLA && bash smoke/setup_npu_env.sh`（幂等，重建 NPU 可用环境）。

---

**汇报路径**：本文提交进仓库，待 GitHub 凭据补上后 push；用户将同步转达 Fable 5。
