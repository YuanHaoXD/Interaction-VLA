# M1-B4 样例观感记录

> 任务：100 段端到端样例数据 + 抽查回放（服务器 A5 对接基准）
> 环境：本机 Windows 11 仿真 daemon（`--sim --scene minimal`）
> 执行：2026-07-14，B 系列本机侧
> 产出：`data_gen/make_samples.py`、`samples/ep_00000000` ~ `ep_00000099`（100 段）

## 1. 数据生成结果

```
生成并校验通过 100/100 段 → samples/
```

- 100 段全部通过 `validate_episode` 校验（0 失败）。
- 每段结构：`meta.json` + `timeline.json` + `actions.npy`（无音频/无帧，符合无音频原则与样例定位）。
- 时长分布：15~40 秒随机（帧数 455~1194 @30Hz）。
- **限幅合规**：全 100 段逐维检查，safe_min/safe_max 越界段数 = 0。
- **限速合规**：全 100 段逐帧检查，max_vel 越界段数 = 0（连续性有保证）。
- 数值健康：抽查无 NaN，dtype=float32。

## 2. 抽查回放（肉眼验收，用户口述）

起仿真 daemon，回放 3 段代表性样例（覆盖 4 类事件标签 + idle + backchannel）：

| episode | 时长 | 事件标签 | 30Hz 时序 |
|---|---|---|---|
| ep_00000000 | 19.5s | wiggle_antennas | 实际 19.5s = 标称 ✅ |
| ep_00000003 | 24.0s | shake_head + nod | 实际 24.0s = 标称 ✅ |
| ep_00000004 | 28.7s | nod + tilt_head | 实际 28.7s = 标称 ✅ |

回放器时序精确（忙等对齐，实际用时与标称完全一致）。

**用户肉眼观感（三点验收全部通过）**：

1. **呼吸感（idle 微动）**：✅ 有呼吸感/活着感——事件之外的时间，头部/天线有持续的细微漂移，不是死沉沉定住。
2. **事件动作**：✅ 明显、到位——点头/摇头/歪头/天线摆动都清晰可辨，幅度恰当。
3. **连续性**：✅ 流畅无跳变——无突然跳变、无穿模、无僵在极限位置发抖。

## 3. 基线结论与后续调参线索

- **当前模板参数作为基线合格**：拟人感三要素（活着感、事件表达、流畅度）均达标，可作为服务器 A5 规模化数据生成的对接基准。
- idle 微动幅度（z 呼吸 ±3mm、rpy 慢漂 ±0.015~0.02rad）在仿真里观感自然，未淹没事件动作也未过弱。
- 未发现需立即调整的参数问题。后续若在真机或更长序列上回放，重点复查：① idle 与事件叠加处是否偶发越界后被裁剪导致的轻微顿挫（本轮限速逐帧裁剪已消除，未观察到）；② backchannel 点头在密集 user_text 区间是否过频。

## 4. 待决问题

无 B 侧大事待决。样例数据质量合格，交付服务器 A5 对接。

## 5. 产出文件

- `data_gen/make_samples.py`——样例生成脚本（照计划实现）
- `samples/ep_00000000` ~ `ep_00000099`——100 段合法 episode（入库，服务器 A5 对接样本）
